stages:
  - build
  - deploy

variables:
  DOCKER_IMAGE_FULL_NAME: "$DOCKERHUB_USERNAME/$DOCKERHUB_REPO:$CI_COMMIT_SHORT_SHA"
  # 호스트의 도커 소켓을 사용하도록 강제
  DOCKER_HOST: ""       
  DOCKER_TLS_CERTDIR: ""

.ai-dev-rules:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event" && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == "ai-dev"'
    - if: '$CI_COMMIT_BRANCH == "ai-dev"'

build_push_dockerhub:
  stage: build
  image: docker:latest 
  

  before_script:
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  script:
    - echo "📂 ProjecTree-Inference 폴더로 이동합니다."
    - cd ProjecTree-Inference
    - echo "🐳 Docker Hub 이미지를 빌드합니다..."
    - docker build -t $DOCKER_IMAGE_FULL_NAME .
    - docker push $DOCKER_IMAGE_FULL_NAME
  extends: .ai-dev-rules


deploy_to_aws:
  stage: deploy
  image: docker:latest  
  
  script:
    - echo "🚀 AWS 서버 내부에서 직접 배포를 시작합니다."
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
    
    - docker pull $DOCKER_IMAGE_FULL_NAME
    
    - docker stop inference-app || true
    - docker rm inference-app || true
    
    - docker run -d --name inference-app --env-file ./ai.env -p 8000:8000 $DOCKER_IMAGE_FULL_NAME
    
    - docker image prune -f
    
  extends: .ai-dev-rules